# Multica web frontend for Railway. Same tag as Dockerfile.
# Reads REMOTE_API_URL, DOCS_URL and NEXT_PUBLIC_WS_URL at runtime; listens on $PORT / $HOSTNAME.
FROM ghcr.io/multica-ai/multica-web:v0.4.41
